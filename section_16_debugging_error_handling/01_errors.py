# Errors

"""
Errors:
    --> 1. SyntaxError:
                --> Occurs When Python Encounters Incorrect Syntax(Something it does not parse).
                --> Usually due to typos or not knowing python well enough.
    --> 2.  NameError:
                --> This occurs when a variable is not defined, i.e. it hasn't been assigned.
    --> 3.  TypeError:
                --> Occurs When An operation or function is applied to the wrong type or python cannot interpret an operation on two data types.
    --> 4.  IndexError:
                --> Occurs when we try to access an element in a list using an invalid index (i.e. one that is outside the range of the list, tuple or string).
    --> 5.  ValueError:
                --> This occrus when a built-in operation or function receives an argument that has the right type but an inappropriate value.
    --> 6.  KeyError:
                --> This occurs when a dictionary does not have a specific key.
    --> 7.  AttributeError:
                --> This occurs when a variable does not have an attribute.            
"""